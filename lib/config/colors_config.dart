import 'package:flutter/material.dart' show Color, Colors;

class ColorsConfig {
  static const primary = Colors.black;
  static const secondary = Color.fromARGB(255, 58, 90, 152);
  static const red = Colors.red;
  static const white = Colors.white;
}
